[project]
name = "cinema-catalog"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.128.8",
    "mypy>=1.11",
    "python-multipart>=0.0.20",
    "uvicorn>=0.39.0",
]
